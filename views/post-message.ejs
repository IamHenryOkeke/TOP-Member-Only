<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="/styles.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Post Message</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <main>
    <%- include('navbar') %>
    <div class="container md:w-3/5 mx-auto mt-10">
      <form action="/message/post" method="post" id="form" class="w-full mx-auto">
          <h2 class="text-2xl font-semibold">Post Message</h2>
          <div class="input-group">
              <label for="title">Title</label>
              <input value="<%= (locals.data && locals.data.title) ? locals.data.title : '' %>" name="title" id="title" placeholder="Enter message title" required>
          </div>
          <div class="input-group">
              <label for="message">Message</label>
              <textarea class="w-full rounded-md resize-none p-2 focus:outline-none h-32 bg-[#282c34]" value="<%= (locals.data && locals.data.message) ? locals.data.message : '' %>" name="message" id="message" placeholder="Enter message" required></textarea>
          </div>
          <button type="submit">Post Message</button>
          <%- include('errors') %>
      </form>
  </div>
  </main>
</body>
</html>