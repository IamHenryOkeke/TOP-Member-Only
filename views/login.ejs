<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="/styles.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign Up</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <main>
    <%- include('navbar') %>
    <div class="container md:w-3/5 mx-auto  mt-10">
      <form action="/auth/login" method="post" id="form" class="w-full mx-auto">
          <h2 class="text-2xl font-semibold">Login</h2>
          <div class="input-group">
              <label for="email">Email</label>
              <input type="email" value="<%= (locals.data && locals.data.email) ? locals.data.email : '' %>" name="email" id="email" placeholder="Enter your email" required>
          </div>
          <div class="input-group">
              <label for="password">Password</label>
              <input type="password" value="<%= (locals.data && locals.data.password) ? locals.data.password : '' %>" name="password" id="password" placeholder="Create a password" required>
          </div>
          <button type="submit">Log in</button>
      </form>
  </div>
  </main>
</body>
</html>